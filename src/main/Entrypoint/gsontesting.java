package Entrypoint;

import com.google.gson.Gson;
import com.google.gson.GsonBuilder;


public class gsontesting {
    public static void main(String[] args) {
//        $body = @{
//>>     CompleteSolution = "class SimpleCalculator { public static int addTwoNumbers(int num1, int num2){ return num1 + num2; } }"
//                    >>     TestUnit = "import org.junit.jupiter.api.Test; import static org.junit.jupiter.api.Assertions.*; class SimpleCalculatorTest { @Test void positivePlusPositiveEqualsPositive(){ var calculator = new SimpleCalculator(); assertEquals(300, calculator.addTwoNumbers(100,200));}}"
//                    >>     EdgeCase = 'import org.junit.jupiter.api.Test; import static org.junit.jupiter.api.Assertions.*; class SimpleCalculatorTest { @Test void positivePlusPositiveEqualsPositive(){ var calculator = new SimpleCalculator(); assertEquals(300, calculator.addTwoNumbers(100,200)); System.out.println("Positive plus positive is indeed positive"); } @Test void negativePlusNegativeEqualsNegative(){ var calculator = new SimpleCalculator(); assertEquals(-200, calculator.addTwoNumbers(-100,-100)); } }'
//                    >> } | ConvertTo-Json -Depth 3
    }
}
